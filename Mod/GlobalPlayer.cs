using Terraria;
using Terraria.DataStructures;
using Terraria.ModLoader;

namespace RL_API
{
    /*public class NoRenderGlobalPlayer : Player
    {
        public override bool IsHeadLayer => false; // This is not a head layer



        protected override void Draw(ref PlayerDrawSet drawInfo)
        {
            // Skip rendering for all players
            drawInfo.drawPlayer.skipAnimatingValuesInPlayerFrame = true;
            //drawInfo.drawPlayer.hide
        }

        public override Position GetDefaultPosition()
        {
            // Return the default position for the player
            return new AfterParent(PlayerDrawLayers.Torso);
        }
    }*/
}